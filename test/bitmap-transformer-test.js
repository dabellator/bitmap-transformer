'use strict';

var expect = require('chai').expect;

var transform = require(__dirname + '/../lib/bitmap-transformer.js')

describe('bitmap-transformer.js', function(){
  it('should take in a bmp file and create a new bmp file', function(){
    expect().to.deep.equal("BM");
  });
});
